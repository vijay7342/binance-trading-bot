# Binance Futures Trading Bot (Testnet)

## Description
A Python-based trading bot that connects to Binance Futures Testnet.  
It supports placing MARKET and LIMIT orders and logs all activity in `trading.log`.

## Setup
1. Clone the repository or unzip the folder.
2. Create a `.env` file in the project root with:
3. Install dependencies:


## Usage


## Output
- All order requests and responses are saved in `trading.log`.

## Notes
- Works only with Binance Futures Testnet keys.
- Do not upload `.env` to GitHub.
